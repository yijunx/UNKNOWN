import numpy as np
import sys

a = np.array([1, 2, 3])
c = np.array([7,8,9])
b = np.array([[1.0, 2, 3], [4, 5, 6]])

# ones, zeros, full, full_like, np.random.rand(4,2), np.random.randint(-4, 8, size=(3,3))
# arr = np.array([[1,2,3]])
# b= a and  b = a.copy()
# can be scalar operation with each element


# reshape, vstack, hstack((arr1, arr2))


# np.getfromtxt(.. delimiter=',')


# arr[arr > 50] also works
# np.any(arr>50, axis=0)  # can also np.all(

# ~ also means reverse..


